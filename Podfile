# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WickedRideDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'TwitterKit'
  # Pods for WickedRideDemo

  target 'WickedRideDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WickedRideDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
